// Illustrative interface previews for projects with no working live demo to
// screenshot (their old PHP deployments serve raw source, not rendered
// HTML — see project case-study content). Clearly a schematic
// representation, not a captured screenshot.
export const projectMockups: Record<string, "booking" | "ecommerce"> = {
  "hotel-booking-website": "booking",
  "butcher-shop": "ecommerce",
};
